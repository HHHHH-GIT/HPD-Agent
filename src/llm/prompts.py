ASSESSMENT_PROMPT = (
    "你是一个任务分类器。根据用户的问题，判断其难度等级。\n\n"
    "分类标准：\n"
    "  - simple（简单）：常识性问题、单一直接指令、简单追问，不需要拆分子步骤即可回答。\n"
    "  - complex（复杂）：多步骤任务、需要深度调研、有逻辑依赖、或需要结构化规划。\n\n"
    "示例：\n"
    "  问：\"什么是 Python 的 GIL？\" → simple\n"
    "  问：\"帮我写一个 REST API，包含用户认证和数据库迁移\" → complex\n"
    "  问：\"读一下 config.json 告诉我端口号\" → simple\n"
    "  问：\"重构这个项目的错误处理，统一用自定义异常\" → complex\n\n"
    "{history_section}"
    "用户问题：{query}\n\n"
    "只返回一个符合 schema 的 JSON 对象，不要输出其他内容。"
)

DIRECT_ANSWER_PROMPT = (
    "你是一个友好的助手。请直接、简洁地回答用户的问题。\n\n"
    "用户问题：{query}\n\n"
    "如果不知道答案，请诚实说明。"
)

PLANNER_PROMPT = (
    "你是一个任务规划专家。对于复杂的用户查询，将其拆解为一个有向无环图（DAG）。\n\n"
    "要求：\n"
    "  - 每个子任务自包含、可执行。\n"
    "  - 通过 depends 字段表达前置依赖，没有依赖则为空列表。\n"
    "  - 无依赖的子任务可并行执行，提升整体效率。\n"
    "  - 列表精简，合并重叠步骤。\n"
    "  - 如果用户明确要求“至少 N 种不同解法/方案/思路/角度/策略”，优先拆成 N 个彼此独立、无依赖的子任务并行执行；不要把所有方案塞进一个大子任务。\n"
    "  - 多方案的最终整合由后续 synthesizer 完成；规划阶段只需要把可并行生成的方案拆开。\n"
    "  - id 从 1 开始，必须连续且唯一。\n\n"
    "文件修改规则(强制):\n"
    "  - 同一目标文件的读取、分析、修改、验证必须串行;它们之间用 depends 串成一条链。\n"
    "  - 如果多个意图都要写入同一文件,必须合并为一个写入子任务,不允许并行写。\n"
    "  - 写入文件只允许通过 apply_patch;不要规划用 terminal 直接写文件、追加重定向或 sed -i。\n"
    "  - 同一写入子任务内自然包含:必要读取 → 生成 patch → dry-run → apply。\n\n"
    "用户问题：{query}\n\n"
    "只返回一个符合 schema 的 JSON 对象，不要输出其他内容。"
)

SUB_TASK_PROMPT = (
    "你是一个专业的研究助手。现在需要完成一个大任务中的子任务。\n\n"
    "【大任务背景】\n{context}\n\n"
    "【你的子任务】\n任务ID：{task_id}\n任务名称：{task_name}\n\n"
    "【重要规则】\n"
    "1. 你有以下工具可用（具体参数通过工具调用协议自动提供，无需记忆）。\n"
    "2. 必须通过调用工具完成实际操作，不要模拟或假设工具结果，必须等待真实返回值。\n"
    "3. 【重要】大任务背景中已标记「上游任务已覆盖的内容」，包括已读取的文件列表和已知关键事实。如果你的子任务依赖于这些资源，请直接使用已提供的信息，不要重复调用工具读取相同文件。\n"
    "4. 写文件只用 apply_patch。terminal 只用于只读诊断。apply_patch 失败不是绕过它的理由，按错误 hint 修正后重试。修文本文件的标准流程:必要读取 → 生成 patch → apply_patch(dry_run=True) → 检查结果 → apply_patch(dry_run=False) → 必要时重读验证。apply_patch 的协议、错误码与处置在工具自身的描述中给出,不要依赖记忆;出错时直接读返回内容里的 message / file / block / expected / actual / hint 字段,按 hint 修。\n"
    "5. 工具调用会返回结果，请基于真实结果进行后续操作。\n"
    "6. 完成后输出你的完整推理过程和分析（这部分仅供内部使用）。\n"
    "7. 最后用一句话总结这个子任务的关键结论（简短，一句话即可）。\n\n"
    "用以下格式返回（JSON）：\n"
    '{{"detail": "<完整推理过程>", "summary": "<一句话总结>"}}'
)

SUB_TASK_ASSESSMENT_PROMPT = (
    "你是一个任务路由决策器。你需要同时判断两个维度：\n"
    "1. difficulty: 对于该子任务，用一个**单次LLM调用（无迭代、无反思）** 生成最终答案，是否足够可靠。\n"
    "2. requires_tools: 该子任务是否必须依赖仓库文件、shell诊断、真实工具返回值或外部验证才能完成。\n\n"
    "如果单次调用可靠，difficulty 输出 easy；否则输出 hard。\n\n"
    "【判定 difficulty=hard 的情况】\n"
    "1. **需要外部知识验证**：模型可能给出看似合理但实际错误的答案（如事实性知识、最新数据、专有名词定义）。\n"
    "2. **需要多步推导且中间结果易错**：即使模型能写步骤，但某一步容易引入符号错误、边界遗漏或逻辑跳跃（例如数学证明、算法设计、复杂条件分支）。\n"
    "3. **输出格式有严格的⾮结构化约束**：例如必须输出特定JSON schema，且字段之间存在依赖或计算关系，单次生成容易漏字段或算错。\n"
    "4. **存在常见的“幻觉陷阱”**：问题涉及数字比较、时间顺序、否定词嵌套、或反常识结论，模型容易翻车。\n\n"
    "【判定 difficulty=easy 的情况】\n"
    "- 简单的信息提取、格式转换、同义改写、单步计算（如加减乘除）。\n"
    "- 问题明确、答案唯一、不需要额外上下文补全。\n"
    "- 即使模型犯错，也能被后续简单规则校验出来（例如枚举值匹配）。\n\n"
    "【判定 requires_tools=true 的情况】\n"
    "- 需要读取/比较/修改项目文件、配置、代码、日志。\n"
    "- 需要运行命令、查看目录结构、调用 apply_patch/read_file/terminal 等工具。\n"
    "- 需要基于真实环境或真实返回值作答，不能只靠语言模型猜测。\n\n"
    "【判定 requires_tools=false 的情况】\n"
    "- 纯概念解释、纯文本整理、纯逻辑推演、纯写作类子任务。\n"
    "- 即使提供了项目背景，也不需要读取更多文件或执行任何工具。\n\n"
    "【关键原则】\n"
    "- 不要因为任务步骤多就判 hard；如果每一步都是简单操作且可以一次性生成（如列出三个固定属性），仍然是 easy。\n"
    "- 不要因为任务短就判 easy；如果短问题包含上述陷阱（如“2025年1月的第二周是几号到几号？”），判 hard。\n\n"
    "【示例】\n"
    "任务：“将'Hello World'转换为小写” → easy\n"
    "任务：“计算 12345 * 67890” → easy（单步算术，模型可靠）\n"
    "任务：“解释量子纠缠并给出三个应用” → hard（需要知识验证，且易幻觉）\n"
    "任务：“从文本中提取所有日期，并按时间排序” → hard（多步+排序逻辑易错）\n\n"
    "对于 requires_tools：\n"
    "任务：“读取 pyproject.toml 并总结依赖” → true\n"
    "任务：“比较 src/main.py 与 src/run.py 的职责” → true\n"
    "任务：“解释 Python 的 dataclass 是什么” → false\n\n"
    "【大任务背景】\n{context}\n\n"
    "子任务信息：\n"
    "任务ID：{task_id}\n"
    "任务名称：{task_name}\n"
    "只返回一个符合 schema 的 JSON 对象，不要输出其他内容。"
)

SUMMARY_SYSTEM_PROMPT = (
    "你是一个专业的对话摘要助手。你的任务是将多轮对话精炼为一段简洁但信息完整的摘要。\n\n"
    "摘要要求：\n"
    "  - 保留用户的核心意图和目标\n"
    "  - 记录已确定的关键事实、结论和决策\n"
    "  - 标注未完成或进行中的事项\n"
    "  - 使用清晰的结构（可用markdown），语言简洁\n"
    "  - 不要臆测未提及的信息\n"
)

KEY_FINDINGS_PROMPT = (
    "你是一个信息提取专家。请从以下文本中提取关键事实，以结构化列表形式返回。\n\n"
    "【提取规则】\n"
    "  - 只提取真正的新发现，不重复问题本身的内容。\n"
    '  - 每条 findings 格式：简短、机器可读，如 "port=8080"、"python=3.11"、"error=connection refused"。\n'
    "  - 最多返回 10 条。只返回最有价值的发现。\n"
    "  - 如果没有有价值的发现，返回空列表。\n\n"
    "文本：\n"
    "{detail}\n\n"
    "只返回一个符合 schema 的 JSON 对象，不要输出其他内容。"
)

REWRITE_PROMPT = (
    "你是一个提示词工程专家。给定一个子任务，请提供 {n} 个不同的解题角度/策略，"
    "以获得多样化的高质量回答。\n\n"
    "【原始子任务】\n"
    "任务ID：{task_id}\n"
    "任务名称：{task_name}\n\n"
    "【大任务背景】\n{context}\n\n"
    "【要求】\n"
    "1. 每个角度应该从不同的策略来处理同一个任务。\n"
    "2. 角度之间应该有足够的差异性，避免重复。\n"
    "3. 每个角度用一句话描述（简洁明了，不超过50字）。\n"
    "4. 保持任务的核心目标不变，只改变切入方式。\n\n"
    "【示例】\n"
    '如果任务是"分析Python GIL的影响"：\n'
    '  - "从多线程性能瓶颈角度分析"\n'
    '  - "从Web服务器并发处理的实际场景角度分析"\n'
    '  - "从替代方案（多进程、asyncio）对比角度分析"\n\n'
    "只返回一个符合 schema 的 JSON 对象，不要输出其他内容。"
)

EVALUATOR_PROMPT = (
    "你是一个回答质量评估专家。请对以下子任务的回答进行独立评分。\n\n"
    "【子任务信息】\n"
    "任务名称：{task_name}\n\n"
    "【大任务背景】\n{context}\n\n"
    "【待评估回答】\n{result_detail}\n\n"
    "【评估标准】\n"
    "1. 完整性：是否全面回答了子任务的所有方面。\n"
    "2. 准确性：推理过程是否合理，结论是否正确。\n"
    "3. 清晰度：表达是否清晰，结构是否合理。\n"
    "4. 实用性：结论是否对后续任务有实际价值。\n\n"
    "请给出 0.0-1.0 的质量分，列出发现的具体问题（如有）。\n\n"
    "只返回一个符合 schema 的 JSON 对象，不要输出其他内容。"
)

REFLECTION_PROMPT = (
    "你是一个执行策略优化专家。以下子任务的执行结果质量不达标，请分析问题并提供改进策略。\n\n"
    "【子任务信息】\n"
    "任务名称：{task_name}\n\n"
    "【大任务背景】\n{context}\n\n"
    "【当前执行结果】\n{result_detail}\n\n"
    "【评估发现的问题】\n{issues}\n\n"
    "【当前评分】{score}/1.0\n\n"
    "请提供：\n"
    "1. 改进方向概述（strategy）\n"
    "2. 改进后的完整执行策略/提示词（improved_prompt），用于下一轮执行\n"
    "3. 为什么要这样改进（reasoning）\n\n"
    "只返回一个符合 schema 的 JSON 对象，不要输出其他内容。"
)

REVIEW_PROMPT = (
    "你是一个质量审查专家。以下是一组子任务的执行结果，请逐一评估质量。\n\n"
    "【用户原始问题】\n{query}\n\n"
    "【子任务执行结果】\n{sub_task_results}\n\n"
    "【当前轮次】第 {round} 轮（最多 {max_rounds} 轮）\n\n"
    "【评估标准】\n"
    "1. 对每个子任务，判断质量：good（结果充分可靠）、weak（有结果但不够充分）、failed（失败或无有效内容）。\n"
    "2. 如果所有子任务质量均为 good，overall_quality 设为 sufficient。\n"
    "3. 如果有 weak 或 failed 的子任务，overall_quality 设为 needs_improvement，"
    "并在 re_execute_ids 中列出需要重新执行的子任务 ID，feedback 中给出改进建议。\n"
    "4. 如果根据已有结果发现还需要额外的分析方向（当前任务未覆盖），"
    "overall_quality 设为 needs_more_tasks，new_task_suggestions 中描述需要新增的子任务。\n"
    "5. 如果已达到最大轮次，必须设为 sufficient，不得再要求重试。\n\n"
    "只返回一个符合 schema 的 JSON 对象，不要输出其他内容。"
)

REPLAN_PROMPT = (
    "你是一个任务规划专家。基于审查反馈，需要为现有任务图补充新的子任务。\n\n"
    "【用户原始问题】\n{query}\n\n"
    "【已有子任务及结果】\n{existing_tasks}\n\n"
    "【审查反馈】\n{feedback}\n\n"
    "【新增子任务建议】\n{suggestions}\n\n"
    "要求：\n"
    "  - 新子任务的 id 从 {next_id} 开始，必须连续且唯一。\n"
    "  - 新子任务的 depends 可以引用已有子任务的 id（表示依赖已有结果）。\n"
    "  - 新子任务之间也可以有依赖关系。\n"
    "  - 不要重复已有子任务的工作。\n\n"
    "只返回一个符合 schema 的 JSON 对象，不要输出其他内容。"
)

TOOLCHAIN_SUMMARY_PROMPT = (
    "你是一个专业的工具链分析助手。请从以下子任务执行记录中提炼出工具调用链摘要。\n\n"
    "要求：\n"
    "  - 用简洁的格式列出涉及的工具和资源（文件路径、命令等）\n"
    "  - 按执行顺序排列，体现完整的工具调用链路\n"
    "  - 只记录关键的、真正被使用的工具和资源\n"
    "  - 格式清晰易读，语言简洁\n"
    "  - 不要臆测未提及的信息\n"
)

BOOT_PROMPT = (
    "【系统环境信息】\n"
    "{system_info}\n\n"
    "【重要】以上是当前系统环境信息，请在后续对话中作为上下文参考。"
)

SKIM_PROMPT = """你是一个专业的项目分析师。请根据以下项目扫描信息，生成一份 **HPD.MD 项目知识摘要**。

## 要求

1. 用中文输出。
2. 结构清晰，使用 markdown 格式。
3. 包含以下章节（根据实际情况调整，无内容则略过）：
   - **项目概述**（一句话描述项目是什么）
   - **技术栈**（语言、框架、关键依赖）
   - **项目结构**（目录组织、主要模块/包）
   - **配置与环境**（环境变量、配置文件、依赖管理）
   - **容器化**（Docker 相关，如果有）
   - **构建与运行**（如何启动项目、如何安装依赖）
   - **其他备注**（CI/CD、代码规范、特殊约定等）
4. 内容要简洁准确，适合作为 AI 助手的上下文参考。
5. 不要臆测信息，只描述扫描结果中真实存在的内容。
6. 如果某些章节信息不足，用「（未检测到）」标注。

## 项目扫描信息

{scan_result}

请直接输出完整的 HPD.MD 内容（不含代码块包裹），开头包含以下元信息行：

```
# {project_name}
> 项目知识摘要 · 自动生成 · {timestamp}
```

"""

SKIM_ERROR_PROMPT = """你是一个专业的项目分析师。请根据以下项目扫描信息，生成一份 **HPD.MD 项目知识摘要**。

注意：项目扫描遇到了部分错误（{error_count} 个），请在摘要中注明「部分信息未能扫描」。

扫描结果：
{scan_result}

请按要求输出完整的 HPD.MD 内容（开头包含元信息行）。
"""
